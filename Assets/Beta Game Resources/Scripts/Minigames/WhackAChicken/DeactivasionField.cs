using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeactivasionField : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*void OnTriggerStay(Collider other)
    {
        if(other.gameObject.tag == "Mole")
        {
            
            Debug.Log("Yes tag is Mole");

            if (other.gameObject.GetComponent<Mole>() != null)
            {
                Mole mole = other.gameObject.GetComponent<Mole>();

                mole.ColliderDeactivate();
                Debug.Log("DEACTIVATE");
            }
            
        }
        
        
    }*/

    
}
